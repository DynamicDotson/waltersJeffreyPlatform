using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class EnemyPointer : MonoBehaviour
{

    //private Vector3 targetPosition;
    //private RectTransform pointerRectTransform;

    private void Awaken()
    {
        //targetPosition = new Vector3(200, 45);
        //pointerRectTransform = pointerRectTransform.Find("Death_Point").GetComponent<pointerRectTransform>();

    }

    private void Update()
    {
        //Vector3 toPosition = targetPosition;
        //Vector3 fromPosition = Camera.main.transfom.position;
       // fromPosition.z = 0f;
        //Vector3 dir = (toPosition - fromPosition).normalized;
        //UtilsClass.GetAngleFromVectorFloat(dir);
    }
}
